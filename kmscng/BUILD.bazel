load("@bazel_skylib//lib:selects.bzl", "selects")
load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library", "cc_test")

package(default_visibility = ["//kmscng:__subpackages__"])

cc_library(
    name = "cng_headers",
    hdrs = ["cng_headers.h"],
    deps = ["@cpdk//:ncrypt_provider"],
)

cc_library(
    name = "provider",
    hdrs = ["provider.h"],
)

cc_library(
    name = "version",
    hdrs = ["version.h"],
)
